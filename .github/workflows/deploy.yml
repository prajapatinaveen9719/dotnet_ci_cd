name: Deploy .NET API to IIS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: Stop IIS
        run: |
          Import-Module WebAdministration
          Stop-WebAppPool -Name "MonitoringStackAPI"

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build -c Release --no-restore

      - name: Clean IIS Folder
        run: Remove-Item "C:\inetpub\monitoring_api\*" -Recurse -Force

      - name: Publish
        run: dotnet publish -c Release -o "C:\inetpub\monitoring_api" --no-build

      - name: Start IIS
        run: |
          Import-Module WebAdministration
          Start-WebAppPool -Name "MonitoringStackAPI"